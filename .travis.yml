language: python

python:
   - "3.6"
   - "3.7"
   - "3.8"

install:
   #- pip install .
   - pip install pytest

script:
   - pytest

notifications:
   email:
      on_success: change
      on_failure: always
